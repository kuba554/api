<form method="post" action="{{ path('app_product_stock_delete', {'id': product_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_stock.id) }}">
    <button class="btn">Delete</button>
</form>
