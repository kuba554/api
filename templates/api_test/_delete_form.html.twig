<form method="post" action="{{ path('app_api_test_delete', {'id': api_test.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ api_test.id) }}">
    <button class="btn">Delete</button>
</form>
