<form method="post" action="{{ path('app_categories_tree_delete', {'id': categories_tree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_tree.id) }}">
    <button class="btn">Delete</button>
</form>
