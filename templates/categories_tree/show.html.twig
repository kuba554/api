{% extends 'base.html.twig' %}

{% block title %}CategoriesTree{% endblock %}

{% block body %}
    <h1>CategoriesTree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_tree.id }}</td>
            </tr>
            <tr>
                <th>Tree_id</th>
                <td>{{ categories_tree.treeId }}</td>
            </tr>
            <tr>
                <th>Children</th>
                {# <td>{{ categories_tree.children ? categories_tree.children|join(', ') : '' }}</td> #}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categories_tree_index') }}">back to list</a>

    <a href="{{ path('app_categories_tree_edit', {'id': categories_tree.id}) }}">edit</a>

    {{ include('categories_tree/_delete_form.html.twig') }}
{% endblock %}
